[project]
name = "workbench-coordinator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["workbench-common", "workbench-contracts"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
