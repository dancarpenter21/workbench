[project]
name = "workbench-voice"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["workbench-common", "workbench-contracts"]
[project.optional-dependencies]
hardware = ["faster-whisper>=1.1,<2"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
