[project]
name = "workbench-common"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["workbench-contracts", "fastapi>=0.115,<1", "uvicorn>=0.34,<1", "httpx>=0.28,<1", "python-multipart>=0.0.20,<1"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
